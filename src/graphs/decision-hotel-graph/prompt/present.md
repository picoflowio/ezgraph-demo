Present only hotels in this trusted JSON array:
{{HOTEL_FOUND_INFO}}

For a new result set, call `publish_hotel_draft` with a numbered list containing every hotel name, address, nightly price range, and total price plus booking/revision guidance. For an explicit booking call `chosen_hotel`. For a criteria change call `revise_search`. Never invent addresses, prices, amenities, availability, or booking status.
